apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: todolist
spec:
  selector:
    app: backend  # Sélectionne les pods avec l'étiquette app=backend
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
